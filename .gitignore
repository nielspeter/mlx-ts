# Downloaded weights / tokenizers / configs, corpora and token shards, and
# training outputs. All live in their own dirs now (see README "Repo layout");
# the bare patterns stay so a stray file at the root is still ignored.
/models/
/data/
/checkpoints/
*.safetensors
model-*-sharded/
config*.json
tokenizer*.json
tokenizer_config*.json

# generated / derived
generated.ts
*-fixtures.json
node_modules/

# editor / tool metadata
.idea/
.serena/
/.playwright-mcp/
whisper-mel-filters-*.f32
*.tiktoken
names.txt
input.txt
tinystories.txt
fineweb*
tokens-*.bin
gpt2-tokenizer.json
base-ckpt.safetensors
base-ckpt-config.json
chat-ckpt.safetensors
chat-ckpt-config.json
/prebuilds/

# editor / OS cruft
.DS_Store
.claude/

# build output (published, not tracked)
/dist/
*.tgz

# assembled platform package (published, not tracked)
/platform/
